/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,  // React Strict Mode 활성화
  swcMinify: true,  // SWC 기반의 빠른 빌드 압축 활성화
  basePath: '/contact',
};

export default nextConfig;

